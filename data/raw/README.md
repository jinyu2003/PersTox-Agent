# 原始知识库数据

这个目录用于存放 `scripts/download_kb_sources.py` 下载或人工放置的原始数据。

当前阶段只把严格 `T1` 数据源作为正式输入。`T1-T2`、`T2`、`T3` 数据源暂不进入当前知识库构建流程。

下载时尚未完成的文件会以 `.part` 结尾。下载完成后，脚本会写入 `_source_metadata.json`，记录来源、时间、许可说明和文件哈希。
